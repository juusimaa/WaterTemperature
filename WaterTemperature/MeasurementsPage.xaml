<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:WaterTemperature.ViewModels"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="WaterTemperature.MeasurementsPage">
    <!-- BindingContext is set in code-behind -->
    <VerticalStackLayout Spacing="25" Padding="30,0">
        <Label Text="Edit Measurements" FontSize="20" HorizontalOptions="Center" Margin="0,20,0,10" />
        <syncfusion:SfDataGrid
            x:Name="dataGrid"
            ItemsSource="{Binding Measurements}"
            AllowEditing="True"
            EditTapAction="OnTap"
            SelectionMode="Single"
            HeaderRowHeight="40"
            RowHeight="40"
            CellValueChanged="OnDataGridCellValueChanged"
            Margin="0,10,0,0" />
        <Label Text="Tap a cell to edit the value. Tap a row to select, then tap again to delete." FontSize="14" TextColor="Gray" HorizontalOptions="Center" Margin="0,10,0,0" />
    </VerticalStackLayout>
</ContentPage>
